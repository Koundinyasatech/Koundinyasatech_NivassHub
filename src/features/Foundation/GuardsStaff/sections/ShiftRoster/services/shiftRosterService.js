export const shiftRosterService = {
  getAllShifts() {
    console.log(
      "Fetch Shift Roster"
    );
  },

  createShift(data) {
    console.log(
      "Create Shift",
      data
    );
  },

  updateShift(data) {
    console.log(
      "Update Shift",
      data
    );
  },

  deleteShift(id) {
    console.log(
      "Delete Shift",
      id
    );
  },
};